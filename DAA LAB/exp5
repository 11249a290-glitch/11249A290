#include<stdio.h>
int main()
{
    int n,c;
    int is[]
    printf("enter no of items:");
    scanf("%d",&n);
    printf("enter bin capacity:");
    scanf("%d",&c);
    printf("enter item sizes:\n");
    for (int i=0;i<n;i++)
    scanf("%d",&is[i]);
}
